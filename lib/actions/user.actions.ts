'use server';

export const signIn = async () => {
    try {
        //Mutation
    } catch (error) {
        console.error(error)
    }
}

export const signUp = async (userData: SignUpParams) => {
    try {
        //Mutation
    } catch (error) {
        console.error(error)
    }
}